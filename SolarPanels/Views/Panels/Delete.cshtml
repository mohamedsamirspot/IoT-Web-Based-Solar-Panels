@model SolarPanels.Models.Entites.Panel

@{
    ViewData["Title"] = "Delete";
}
    <div style="padding:80px">
        <h1>Delete</h1>

        <h3>Are you sure you want to delete this?</h3>
        <div>
            <h4>Panel</h4>
            <hr />
            <dl class="row">
                <dt class="col-sm-2">
                    @Html.DisplayNameFor(model => model.Panel_ID)
                </dt>
                <dd class="col-sm-10">
                    @Html.DisplayFor(model => model.Panel_ID)
                </dd>
                <dt class="col-sm-2">
                    @Html.DisplayNameFor(model => model.Current_Mode)
                </dt>
                <dd class="col-sm-10">
                    @Html.DisplayFor(model => model.Current_Mode)
                </dd>
            </dl>

            <form asp-action="Delete">
                <input type="hidden" asp-for="Id" />
                <input type="submit" value="Delete" class="btn btn-danger" /> |
                <a asp-action="Index">Back to List</a>
            </form>
        </div>
        </div>